name: Close Stale Issues and PRs

on:
  schedule:
    - cron: "30 1 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: qntx/workflows/.github/workflows/ops-stale.yml@v2
    permissions:
      issues: write
      pull-requests: write
